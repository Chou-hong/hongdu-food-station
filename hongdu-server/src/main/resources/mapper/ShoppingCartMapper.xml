<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hongdu.mapper.ShoppingCartMapper">

    <select id="list" resultType="com.hongdu.entity.ShoppingCart">
        select * from shopping_cart
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="comboId != null">
                and combo_id = #{comboId}
            </if>
            <if test="productId != null">
                and product_id = #{productId}
            </if>
            <if test="productSpec != null">
                and product_spec = #{productSpec}
            </if>
        </where>
    </select>

    <insert id="insertBatch" parameterType="list">
        insert into shopping_cart (name, image, user_id, product_id, combo_id, product_spec, number, amount, create_time)
        values
        <foreach collection="shoppingCartList" item="sc" separator=",">
            (#{sc.name},#{sc.image},#{sc.userId},#{sc.productId},#{sc.comboId},#{sc.productSpec},#{sc.number},#{sc.amount},#{sc.createTime})
        </foreach>
    </insert>
</mapper>
