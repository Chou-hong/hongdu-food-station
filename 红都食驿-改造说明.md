# 红都食驿（Hongdu Food Station）改造说明

> 基于苍穹外卖（sky-take-out）架构重构的本地化餐饮点餐系统，业务模型保留外卖平台的完整闭环，品牌与数据全面替换为「红都食驿」主题。



***

## 一、命名对照表



| 维度   | 原（苍穹外卖）                                        | 现（红都食驿）                                         |
| ---- | ---------------------------------------------- | ----------------------------------------------- |
| 项目名  | sky-take-out / canteen-order-system            | **hongdu-food-station**                         |
| 包名   | com.sky / com.canteen                          | **com.hongdu**                                  |
| 模块   | canteen-common / canteen-pojo / canteen-server | **hongdu-common / hongdu-pojo / hongdu-server** |
| 启动类  | CanteenApplication                             | **HongduApplication**                           |
| 配置前缀 | canteen.                                       | **hongdu.**                                     |
| 数据库  | sky\_take\_out                                 | **hongdu\_food\_station**                       |
| 接口文档 | 苍穹外卖项目接口文档                                     | **红都食驿项目接口文档**                                  |
| 店铺地址 | 北京市海淀区上地十街 10 号                                | **江西省瑞金市红都大道 188 号**                            |

业务概念命名（可复用部分保持不变）：菜品 `product`、套餐 `combo`、套餐明细 `combo_item`、口味 `product_spec`、员工 `staff`、用户 `user`、自提点 / 地址 `pickup_point`、购物车 `shopping_cart`、订单 `orders`、订单明细 `order_detail`、分类 `category`。



***

## 二、技术栈升级（Spring Boot 3.x + JDK 17）



| 组件                             | 升级前                        | 升级后                             | 说明                               |
| ------------------------------ | -------------------------- | ------------------------------- | -------------------------------- |
| Spring Boot                    | 2.7.3                      | **3.2.5**                       | Spring Framework 6 / Tomcat 10.1 |
| JDK                            | 17（配置）                     | **17**                          | 实际以 17 编译运行                      |
| mybatis-spring-boot-starter    | 2.2.0                      | **3.0.3**                       | 适配 Boot 3                        |
| pagehelper-spring-boot-starter | 1.3.0                      | **2.1.0**                       | 适配 Boot 3 /jakarta               |
| druid-spring-boot-starter      | 1.2.1                      | **1.2.23**                      | 适配 Boot 3                        |
| knife4j                        | 3.0.2（springfox）           | **4.5.0**（openapi3-jakarta）     | 接口文档体系升级                         |
| jjwt                           | 0.9.1                      | **0.11.5**（api/impl/jackson）    | 拆分为三件套                           |
| POI                            | 3.16                       | **5.2.5**                       | 报表导出                             |
| fastjson                       | 1.2.76                     | **1.2.83**                      | 1.x 最终安全版本                       |
| aliyun-sdk-oss                 | 3.10.2                     | **3.17.4**                      | JDK17 兼容                         |
| mysql 驱动                       | mysql:mysql-connector-java | **com.mysql:mysql-connector-j** | 新坐标（Boot 3 托管版本）                 |
| jaxb-api                       | javax.xml.bind:jaxb-api    | **移除**                          | jjwt 0.11 不再依赖                   |
| commons-lang3                  | （传递依赖）                     | **3.13.0 显式声明**                 | ReportServiceImpl 使用             |



***

## 三、升级过程中的兼容性问题与解决方案



1. **javax → jakarta 迁移**

* `javax.servlet.*` → `jakarta.servlet.*`（拦截器、报表导出、支付回调，共 6 个文件）

* `javax.websocket.*` → `jakarta.websocket.*`（WebSocketServer）

* 移除 `javax.xml.bind:jaxb-api` 依赖

1. **druid 自动配置失效（关键问题）**

* Spring Boot 3.x 不再读取 `META-INF/spring.factories` 中的自动配置，而 druid-spring-boot-starter 1.2.23 仍只通过该文件注册 `DruidDataSourceAutoConfigure`，导致数据源未创建、启动报 `Failed to configure a DataSource`。

* 解决：新增 `hongdu-server/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`，按 Boot 3 规范手动注册 Druid 自动配置。

1. **springfox → springdoc / OpenAPI3 注解迁移**

* `@Api` → `@Tag(name=...)`；`@ApiOperation` → `@Operation(summary=...)`；`@ApiModel/@ApiModelProperty` → `@Schema(description=...)`（17 个 Controller + 2 个 DTO/VO）

* `WebMvcConfiguration` 由继承 `WebMvcConfigurationSupport` 改为实现 `WebMvcConfigurer`，springfox `Docket` Bean 替换为 springdoc `OpenAPI` Bean，接口分组迁移到 `application.yml` 的 `springdoc.group-configs`。

* 文档访问地址不变：`http://localhost:8080/doc.html`

1. **jjwt 0.11.x API 与密钥长度**

* `Jwts.builder().signWith(SignatureAlgorithm, byte[])` 改为 `Keys.hmacShaKeyFor(...)` 生成 `SecretKey`；`parser()` 改为 `parserBuilder().setSigningKey(...).build()`。对外方法签名（createJWT/parseJWT）保持不变。

* jjwt 0.11 要求 HS256 密钥 ≥ 256 位（32 字节），原 `itcast`/`itheima` 过短会抛 `WeakKeyException`，已在 `application.yml` 中更换为长密钥。

1. **其他**

* POI 3.16 → 5.2.5：报表导出 API 兼容，无需改代码。

* 修复历史遗留：`OrderServiceImpl` 中两处 `${sky.shop.address}` / `${sky.baidu.ak}` 占位符（此前改名遗漏），已改为 `hongdu.*`。



***

## 四、数据库（hongdu\_food\_station）



* 建库脚本：`hongdu_food_station.sql`（已在本机 MySQL 8.0 执行验证）

* 11 张表，结构与 `com.hongdu` 实体类 / `mapper/*.xml` 完全对齐：

  `category`、`product`、`product_spec`、`combo`、`combo_item`、`staff`、`user`、`pickup_point`、`shopping_cart`、`orders`、`order_detail`

* 字符集：utf8mb4（支持完整中文与生僻字）

* 种子数据为红都主题：红都招牌菜、忆苦思甜菜、客家风味、主食简餐、汤羹、饮品 6 类菜品分类 + 人气套餐、商务套餐 2 类套餐分类；19 道菜品（红军焖鸭、瑞金牛肉汤、客家酿豆腐、红米饭、南瓜汤、红军菜、芋子包等）；4 个套餐（忆苦思甜套餐、红都招牌套餐、客家风味套餐、单人简餐）；示例用户 / 自提点 / 订单（便于工作台与报表演示）。

* 管理员账号：`admin` / `123456`（密码以 MD5 存储，`e10adc3949ba59abbe56e057f20f883e`；注意原 sky.sql 存明文会导致登录失败，本库已修正）

* 图片地址为占位符，部署时替换为实际 OSS/CDN 地址。

> `sky.sql`
>
>  为原始参考脚本（保留未动）；
>
> `数据库同步脚本.sql`
>
>  为上一轮改名的历史脚本，已废弃，请以 
>
> `hongdu_food_station.sql`
>
>  为准。



***

## 五、业务闭环



```
管理端：员工登录 → 分类管理 → 菜品管理(含口味) → 套餐管理 → 店铺营业状态

用户端：微信登录 → 浏览分类/菜品/套餐 → 购物车 → 提交订单 → 微信支付 → 订单跟踪

运维端：订单管理(接单/拒单/派送/完成) → 客户催单(WebSocket) → 超时订单定时处理

统计端：工作台概览 → 营业数据/订单/用户/销量TOP10 报表 → Excel 运营报表导出
```

定时任务（`task/`）：超时未支付订单自动取消、派送中订单自动完成、WebSocket 来单提醒。



***

## 六、启动方式



```
\# 1. 建库（MySQL 8.0）

mysql -uroot -p < hongdu\_food\_station.sql

\# 2. 修改 hongdu-server/src/main/resources/application-dev.yml 中的数据库/Redis 配置

\# 3. 构建并启动（需 JDK 17 + Maven 3.6+）

mvn clean package -DskipTests

java -jar hongdu-server/target/hongdu-server-1.0-SNAPSHOT.jar

\# 4. 访问

\#    接口文档：http://localhost:8080/doc.html
```

验证结果（本机实测）：



* `mvn clean compile / package / test-compile` 全部通过

* 应用启动成功（Druid 数据源、Redis、拦截器、定时任务均正常初始化）

* 管理端登录（admin/123456 → JWT）、分类分页查询、工作台数据统计接口实测返回正常

* `/doc.html`、`/v3/api-docs` 返回 200



***

## 七、前端说明

按需求，前端（管理端 Web、用户端小程序）代码本次未处理。接口路径与响应结构保持不变，旧前端可直接对接；前端中的品牌文案（苍穹外卖 → 红都食驿）与图片资源需另行替换。



***

## 八、前端改造（管理端 Web + 用户端小程序）

### 8.1 管理端前端（Vue + TypeScript）

项目路径：D:\BaiduNetdiskDownload\ 苍穹外卖 \project-sky-admin-vue-ts

**API 层重命名**：



* src/api/dish.ts → src/api/product.ts（端点 /dish/\* → /product/\*）

* src/api/setMeal.ts → src/api/combo.ts（端点 /setmeal/\* → /combo/\*）

* src/api/employee.ts → src/api/staff.ts（端点 /employee/\* → /staff/\*）

* src/api/users.ts：/employee/editPassword → /staff/editPassword

* src/api/index.ts：/workspace/overviewDishes → /workspace/overviewProductes，/workspace/overviewSetmeals → /workspace/overviewCombos

* 导出函数名同步重命名（getDishPage→getProductPage、getSetmealPage→getComboPage、getEmployeeList→getStaffList 等）

**视图层重命名**：



* src/views/dish/ → src/views/product/（含 ddDishtype.vue→ddProducttype.vue）

* src/views/setmeal/ → src/views/combo/（含 ddSetmeal.vue→ddCombo.vue、components/AddDish.vue→AddProduct.vue）

* src/views/employee/ → src/views/staff/（含 ddEmployee.vue→ddStaff.vue）

* 路由路径同步：/dish→/product、/setmeal→/combo、/employee→/staff，含 \$router.push 跳转

**数据字段对齐**：



* dishId→productId、setmealId→comboId、dishFlavor→productSpec、setmealDishes→comboItemes、ddressBookId→pickupPointId（全部 .vue/.ts 文件）

**品牌替换**：



* public/index.html：苍穹外卖 → 红都食驿

* src/router.ts：页面标题 苍穹外卖 → 红都食驿

* src/main.ts：注释 瑞吉外卖 → 红都食驿

**构建验证**：

pm run build 成功（需 NODE\_OPTIONS=--openssl-legacy-provider，因 Node 22 + webpack 4 的 OpenSSL 兼容问题）。

### 8.2 用户端小程序（原生微信小程序）

项目路径：C:\Users\15970\IdeaProjects\canteen-miniprogram

> 注：该小程序为 uni-app 编译产物（webpack bundle），API 逻辑集中在 common/vendor.js 中。

**API 端点对齐**（common/vendor.js）：



* /user/dish/list → /user/product/list

* /user/setmeal/list → /user/combo/list

* /user/setmeal/dish/{id} → /user/combo/product/{id}

* /user/addressBook/\* → /user/pickupPoint/\*（list/default/ 增删改查）

* /user/order/userPage → /user/order/historyOrders

**数据字段对齐**（.js + .wxml + .wxss）：



* dishId→productId、setmealId→comboId、dishFlavor→productSpec、ddressBookId→pickupPointId

* CSS 类名 .dish\_dishFlavor → .dish\_productSpec（同步 WXML 与 WXSS）

**品牌替换**：



* pp.json/pages/index/index.json：导航栏标题 校园食堂 → 红都食驿

* 首页 / 订单详情 / 提交订单 / 支付页 WXML：校园食堂 → 红都食驿

* 店铺地址：北京市朝阳区新街大道一号楼 8 层 → 江西省瑞金市红都大道 188 号

* 校内食堂 → 红都食驿、食堂已打烊 → 店铺已打烊

* project.config.json：projectname → hongdu-food-station

**API 基础地址**：[http://localhost:8080](http://localhost:8080)（无需修改，与后端一致）。

### 8.3 后端补充端点



* StaffController 新增 PUT /admin/staff/editPassword（前端修改密码功能调用，原项目遗漏）

* 对应 StaffService.editPassword () / StaffServiceImpl 实现（MD5 校验旧密码 + 更新新密码）

### 8.4 未改动项说明



* 小程序 ppid（wxffb3637a228223b8）为原测试号，部署时需替换为真实小程序 appid

* 微信支付配置（appid/secret/mchid/ 证书路径）沿用原项目测试值，需替换为真实商户配置

* OSS 配置为占位符，需替换为真实阿里云 OSS 凭证

* 小程序 Logo 图片（static/logo\_ruiji.png）为二进制文件，未做修改，如需替换请自行更换

* 管理端 iews/chart/ 和 iews/inform/ 为未在路由中注册的遗留页面（对应后端无 /messages 接口），不影响主流程